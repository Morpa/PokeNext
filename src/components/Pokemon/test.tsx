import { renderWithTheme } from 'utils/tests/helpers'

import { Wrapper } from '.'

describe('<Pokemon />', () => {
  it('should render pokemon page correctly', () => {
    /* const { container } = renderWithTheme(
      <Wrapper>
        <Info>Pokemon</Info>
        <Back>Back</Back>
      </Wrapper>
    ) */
    /*  expect(container.firstChild).toHaveStyleRule('min-width', '35rem')

    expect(container.firstChild?.firstChild).toHaveStyleRule('color', '#FAFAFA')

    expect(container.firstChild).toMatchSnapshot() */
  })
})
